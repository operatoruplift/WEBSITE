import React from 'react';
// This component is deprecated in favor of DownloadWidget.tsx
// Keeping file to prevent import errors in untracked files, 
// but it renders nothing.

const CodeSnippet: React.FC<any> = () => {
  return null;
};

export default CodeSnippet;